import { SquaredThumbnailPipe } from './squared-thumbnail.pipe';

describe('SquaredThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new SquaredThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
