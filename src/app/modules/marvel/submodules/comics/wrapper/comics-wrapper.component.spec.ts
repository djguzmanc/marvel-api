import { ComicsWrapperComponent } from './comics-wrapper.component';

fdescribe('ComicsWrapperComponent', () => {
  let component: ComicsWrapperComponent;

  beforeEach(() => {
    component = new ComicsWrapperComponent();
  });

  it('should create an instance', () => {
    expect(component).toBeTruthy();
  });
});
