import { MarvelWrapperComponent } from './marvel-wrapper.component';

fdescribe('MarvelWrapperComponent', () => {
  let component: MarvelWrapperComponent;

  beforeEach(() => {
    component = new MarvelWrapperComponent();
  });

  it('should create an instance', () => {
    expect(component).toBeTruthy();
  });
});
